{
  /* Hiển thị danh sách tệp đã chọn */
}
{
  /* {fileList.length > 0 && (
              <div className="mt-4">
                <p className="font-semibold">Các tệp đã chọn:</p>
                <ul className="list-disc pl-5">
                  {fileList.map((file) => (
                    <li key={file.uid} className="text-gray-700">
                      {file.name}
                    </li>
                  ))}
                </ul>
              </div>
            )} */
}

{
  /* Nút Ảnh/Video */
}
{
  /* <label
              htmlFor="fileInput"
              className="flex flex-1 items-center justify-center space-x-2 bg-gray-100 text-gray-600 hover:bg-gray-200 cursor-pointer"
            >
              <i className="fas fa-images text-green-600"></i>
              <span>Ảnh/Video</span>
            </label>
            <input
              id="fileInput"
              type="file"
              accept="image/*,video/*"
              multiple
              style={{ display: "none" }}
              onChange={(e) => handleFileChange(e)}
            /> */
}
{
  /* <Button className="flex flex-1 items-center justify-center space-x-2 bg-gray-100 text-gray-600 hover:bg-gray-200">
              <i className="fas fa-images text-green-600"></i>
              <span>Ảnh/Video</span>
            </Button> */
}
